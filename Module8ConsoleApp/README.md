# Модуль 8. Делегати, події, записи

Цей модуль розглядає важливі концепції C#, які дозволяють створювати гнучкі та потужні програми.

---

## 📋 Зміст

1. [Делегати](#делегати)
2. [Події](#події)
3. [Анонімні методи](#анонімні-методи)
4. [Лямбда-вирази](#лямбда-вирази)
5. [Extension-методи](#extension-методи)
6. [Записи (Records)](#записи-records)

---

## Делегати

### Що таке делегат?

**Делегат** - це тип, який представляє посилання на метод. Це схоже на "вказівник на функцію" в інших мовах програмування. Делегат дозволяє передавати методи як параметри, зберігати їх у змінних та викликати динамічно.

### Синтаксис оголошення делегата

```csharp
delegate void MyDelegate(string message);
delegate int CalculateDelegate(int a, int b);
delegate bool PredicateDelegate(int number);
```

### Базовий приклад

```csharp
delegate void GreetingDelegate(string name);

static void SayHello(string name)
{
    Console.WriteLine($"Привіт, {name}!");
}

static void Main()
{
    GreetingDelegate greet = SayHello;
    greet("Іван"); // Виведе: Привіт, Іван!
}
```

### Multicasting (багатоадресність)

Делегат може містити посилання на кілька методів одночасно. При виклику делегата виконуються всі методи в порядку додавання.

```csharp
delegate void NumberDelegate(int number);

NumberDelegate operations = PrintNumber;
operations += DoubleNumber;  // Додаємо метод
operations += SquareNumber;  // Додаємо ще один метод
operations(5);  // Викликаються всі три методи

operations -= DoubleNumber;  // Видаляємо метод
```

### Базові класи для делегатів

- **System.Delegate** - базовий клас для всіх делегатів
- **System.MulticastDelegate** - наслідується від `Delegate`, дозволяє multicast (виклик кількох методів)

### Вбудовані делегати

C# надає готові делегати:

- **Action<T>** - для методів без повернення значення
- **Func<T, TResult>** - для методів з поверненням значення
- **Predicate<T>** - для методів, що повертають bool

```csharp
Action<string> print = (msg) => Console.WriteLine(msg);
Func<int, int, int> add = (a, b) => a + b;
Predicate<int> isEven = (n) => n % 2 == 0;
```

**📁 Практичні приклади:** Дивіться файл `DelegatesExamples.cs`

---

## Події

### Що таке подія?

**Подія** - це спеціальний вид делегата, який дозволяє об'єкту повідомляти інші об'єкти про те, що щось сталося. Події забезпечують безпечний механізм для реалізації патерну "Observer".

### Синтаксис оголошення події

```csharp
public event EventHandler MyEvent;
public event EventHandler<T> CustomEvent;
```

### Базовий приклад

```csharp
class Button
{
    public event EventHandler Clicked;
    
    public void Click()
    {
        Clicked?.Invoke(this, EventArgs.Empty);
    }
}

// Використання
Button button = new Button();
button.Clicked += (sender, e) => Console.WriteLine("Кнопка натиснута!");
button.Click();
```

### Події з параметрами

```csharp
public event EventHandler<TemperatureChangedEventArgs> TemperatureChanged;

class TemperatureChangedEventArgs : EventArgs
{
    public double OldTemperature { get; set; }
    public double NewTemperature { get; set; }
}
```

### Подієві засоби доступу (add/remove)

Можна кастомізувати логіку підписки та відписки від події:

```csharp
private EventHandler _myEvent;

public event EventHandler MyEvent
{
    add { _myEvent += value; }
    remove { _myEvent -= value; }
}
```

**📁 Практичні приклади:** Дивіться файл `EventsExamples.cs`

---

## Анонімні методи

**Анонімний метод** - це метод без імені, який можна визначити безпосередньо в коді.

### Синтаксис

```csharp
delegate void MyDelegate(int x);

MyDelegate del = delegate(int x)
{
    Console.WriteLine($"Значення: {x}");
};

del(10);
```

### Приклад

```csharp
delegate int MathOperation(int a, int b);

MathOperation add = delegate(int a, int b)
{
    return a + b;
};

Console.WriteLine(add(5, 3)); // 8
```

**📁 Практичні приклади:** Дивіться файл `AnonymousMethodsExamples.cs`

---

## Лямбда-вирази

**Лямбда-вираз** - це коротший спосіб запису анонімних методів. Лямбда-вирази є більш читабельними та компактними.

### Синтаксис

```csharp
// Параметри => вираз
(x) => x * 2
(a, b) => a + b
() => Console.WriteLine("Привіт")
```

### Приклади

```csharp
Func<int, int> square = x => x * x;
Func<int, int, int> add = (a, b) => a + b;
Action greet = () => Console.WriteLine("Привіт!");

// З блоком коду
Func<int, int> factorial = n =>
{
    int result = 1;
    for (int i = 1; i <= n; i++)
        result *= i;
    return result;
};
```

### Захоплення змінних (Closure)

Лямбда-вирази можуть захоплювати зовнішні змінні:

```csharp
int baseValue = 10;
Func<int, int> addBase = x => x + baseValue;
Console.WriteLine(addBase(5)); // 15
```

**📁 Практичні приклади:** Дивіться файл `LambdaExpressionsExamples.cs`

---

## Extension-методи

**Extension-методи** дозволяють додавати нові методи до існуючих типів без зміни їх вихідного коду.

### Синтаксис

```csharp
public static class ExtensionClass
{
    public static ReturnType MethodName(this ExtendedType obj, params)
    {
        // Реалізація
    }
}
```

### Приклад

```csharp
public static class StringExtensions
{
    public static string Reverse(this string str)
    {
        char[] chars = str.ToCharArray();
        Array.Reverse(chars);
        return new string(chars);
    }
}

// Використання
string text = "Привіт";
Console.WriteLine(text.Reverse()); // тівірП
```

**📁 Практичні приклади:** Дивіться файл `ExtensionMethodsExamples.cs`

---

## Записи (Records)

### Що таке запис?

**Record** - це новий тип у C# 9.0+, який призначений для представлення незмінних (immutable) даних. Records автоматично генерують корисні методи, такі як `Equals`, `GetHashCode`, `ToString`.

### Синтаксис оголошення

```csharp
// Простий record
public record Person(string FirstName, string LastName);

// Record з додатковими членами
public record Student(string FirstName, string LastName, int StudentId)
{
    public string FullName => $"{FirstName} {LastName}";
}
```

### Порівняння record та class

```csharp
// Клас - порівнює посилання
var person1 = new PersonClass("Іван", "Петренко");
var person2 = new PersonClass("Іван", "Петренко");
Console.WriteLine(person1 == person2); // False

// Record - порівнює значення
var record1 = new PersonRecord("Іван", "Петренко");
var record2 = new PersonRecord("Іван", "Петренко");
Console.WriteLine(record1 == record2); // True
```

### Принципи роботи операторів порівняння

Records використовують **value-based equality** (порівняння за значеннями):

```csharp
Point p1 = new Point(1, 2);
Point p2 = new Point(1, 2);
Console.WriteLine(p1 == p2);  // True (порівнює значення)
Console.WriteLine(p1.Equals(p2)); // True
```

### GetHashCode та Equals

Records автоматично генерують ці методи на основі всіх властивостей:

```csharp
Product product1 = new Product("Ноутбук", 25000, "Електроніка");
Product product2 = new Product("Ноутбук", 25000, "Електроніка");
Console.WriteLine(product1.Equals(product2)); // True
Console.WriteLine(product1.GetHashCode() == product2.GetHashCode()); // True
```

### Наслідування записів

Records підтримують наслідування:

```csharp
public record Person(string FirstName, string LastName);
public record Student(string FirstName, string LastName, int StudentId) 
    : Person(FirstName, LastName);
```

### Незмінність з `with`

```csharp
Person person1 = new Person("Іван", "Петренко", 25);
Person person2 = person1 with { Age = 26 };  // Створює новий record
```

**📁 Практичні приклади:** Дивіться файл `RecordsExamples.cs`

---

## 📝 Підсумок

- **Делегати** - дозволяють працювати з методами як з об'єктами
- **Події** - забезпечують безпечний механізм сповіщення про зміни
- **Анонімні методи** - методи без імені для швидкого визначення
- **Лямбда-вирази** - компактний синтаксис для анонімних методів
- **Extension-методи** - додавання функціональності до існуючих типів
- **Records** - ідеальні для представлення незмінних даних з автоматичною реалізацією Equals, GetHashCode, ToString

---

## 🎯 Практичні поради

1. **Делегати**: Використовуйте вбудовані `Action`, `Func`, `Predicate` замість створення власних делегатів, коли це можливо
2. **Події**: Завжди перевіряйте на `null` перед викликом події (`?.Invoke()`)
3. **Лямбда-вирази**: Віддавайте перевагу лямбда-виразам замість анонімних методів
4. **Extension-методи**: Використовуйте для додавання функціональності до типів, які ви не можете змінити
5. **Records**: Використовуйте для DTO, моделей даних та будь-яких незмінних структур даних

---

**Успіхів у навчанні! 🚀**
