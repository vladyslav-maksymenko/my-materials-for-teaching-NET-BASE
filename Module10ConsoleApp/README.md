# Модуль 10. Збирання сміття (Garbage Collection) в .NET

## Зміст
1. [Життєвий цикл об'єктів](#життєвий-цикл-обєктів)
2. [Поняття складальника сміття](#поняття-складальника-сміття)
3. [Деструкція і метод Finalize](#деструкція-і-метод-finalize)
4. [Метод Dispose та інтерфейс IDisposable](#метод-dispose-та-інтерфейс-idisposable)
5. [Клас System.GC](#клас-systemgc)
6. [Поняття поколінь при збиранні сміття](#поняття-поколінь-при-збиранні-сміття)
7. [Практичні приклади](#практичні-приклади)

---

## Життєвий цикл об'єктів
Практично будь-яка програма для своєї роботи використовує різні об'єкти, робота з якими призводить до витрачання оперативної пам'яті. При об'єктно-орієнтованому підході програмування кожен тип описує якийсь об'єкт, з яким після його створення може працювати програма.

У .NET об'єкти проходять через наступні основні етапи життєвого циклу:

![Схема роботи Generics](PhotoForReadme\lifecycle.jpg)

| Етап | Назва | Опис процесу |
| :--- | :--- | :--- |
| **1** | **Створення (Allocation)** | Об'єкт створюється в керованій купі (**managed heap**) за допомогою оператора `new`. |
| **2** | **Використання (Usage)** | Об'єкт активно використовується в програмі (на нього є посилання). |
| **3** | **Втрата посилань (Unreachable)** | Об'єкт стає недоступним, коли на нього більше не вказує жодна активна змінна. |
| **4** | **Збірка сміття (Garbage Collection)** | **GC** аналізує пам'ять і визначає, що об'єкт можна видалити. |
| **5** | **Фіналізація (Finalization)** | Якщо у класу є деструктор (Finalizer), він викликається перед повним видаленням. |
| **6** | **Видалення (Deallocation)** | Пам'ять звільняється, і місце стає доступним для нових об'єктів. |

---

## Поняття збирача сміття та його поколінь

**Garbage Collector (GC)** - це не просто «прибиральник» або «менеджер», це складна система менеджменту пам'яті, головна мета якої — запобігти Memory Leaks та оптимізувати доступ до даних.
### Де живуть об'єкти? (Stack vs Heap)
Перш за все, GC працює тільки з Managed Heap (керованою купою).
- Stack (Стек): Швидка пам'ять для значущих типів (int, bool, struct) та посилань. Очищається автоматично при виході з методу.
- Managed Heap (Купа): Тут живуть усі посилальні типи (class, string, масиви). Саме тут GC проводить свої «рейди».

Оскільки пам'ять, що виділяється, не безмежна, то необхідно періодично звільняти пам'ять, виділену під об'єкти, які більше не використовуються в додатку (на них немає дійсних посилань). Цю роботу виконує збирач сміття, який є недермінованим процесом, тобто неможливо заздалегідь дізнатися, коли CLR «вирішить» запустити процес збирання сміття у Вашому додатку. Однак, Ви можете примусово запустити збирання сміття, викликавши в додатку метод `Collect()` класу `System.GC`.

### Стратегія трьох поколінь (Generations)
Для оптимізації GC використовує гіпотезу: "Чим довше об'єкт живе, тим більша ймовірність, що він житиме ще довше". Тому купа розділена на три зони:
| Покоління | Назва (метафора) | Опис | Частота перевірки |
| :--- | :--- | :--- | :--- |
| **Gen 0** | 👶 "Дитячий садок" | Сюди потрапляють усі нові об'єкти (крім дуже великих). Це найменша зона за розміром. | **Дуже часто** (кожні кілька мілісекунд). |
| **Gen 1** | 👦 "Підлітки" | Об'єкти, які пережили одну перевірку в Gen 0. Служить буфером перед переходом до "дорослого" життя. | **Середня.** |
| **Gen 2** | 👴 "Старожили" | Об'єкти, що пережили багато збирань, або дуже великі об'єкти. Найбільша зона за обсягом. | **Рідко.** Це називається "повним збиранням" (Full GC). |
Існує ще LOH (Large Object Heap) — спеціальна зона для великих об'єктів (від 85 КБ). Вони одразу потрапляють у Gen 2, бо їх дуже дорого пересувати в пам'яті.

- Відстежує всі об'єкти в managed heap
- Визначає, які об'єкти більше не використовуються
- Автоматично звільняє пам'ять
- Оптимізує використання пам'яті через покоління (generations)

### Переваги GC:
✅ **Автоматичне управління пам'яттю** - немає витоків пам'яті (memory leaks)
✅ **Безпека** - неможливо звернутися до видаленої пам'яті
✅ **Оптимізація** - GC оптимізує розміщення об'єктів

### Недоліки:
⚠️ **Неможливо передбачити точний момент видалення**
⚠️ **Можливі паузи в роботі програми** (хоча в сучасних версіях .NET це мінімізовано)

### Типи збирання сміття в .NET:
- **Workstation GC** - для клієнтських додатків (низька латентність)
- **Server GC** - для серверних додатків (вища пропускна здатність)
- **Background GC** - збірка сміття в фоновому потоці (з .NET 4.0)

---

## Деструкція і метод Finalize

### Теорія

**Деструктор** (finalizer) - це метод, який викликається GC перед видаленням об'єкта.

### Синтаксис:
```csharp
class MyClass
{
    ~MyClass()
    {
        // Код очищення
    }
}
```

### Важливо знати:
- Деструктор **не викликається миттєво** після втрати посилань
- Об'єкт з деструктором проходить через **два етапи збирання сміття**:
  1. Перший етап: об'єкт переміщується в чергу фіналізації
  2. Другий етап: після виконання деструктора об'єкт видаляється
- Об'єкти з деструкторами **живуть довше** (генерація 2)
- **Не рекомендується** використовувати деструктори для звичайних об'єктів
- Використовуйте деструктори **тільки для звільнення неуправляних ресурсів**

### Коли використовувати:
- Робота з неуправляними ресурсами (файли, мережеві з'єднання, дескриптори ОС)
- Як резервний механізм очищення (якщо забули викликати Dispose)

---

## Метод Dispose та інтерфейс IDisposable

### Теорія

**IDisposable** - це інтерфейс, який дозволяє явно звільняти ресурси.

### Паттерн Dispose:

```csharp
public class MyResource : IDisposable
{
    private bool _disposed = false;

    public void Dispose()
    {
        Dispose(true);
        GC.SuppressFinalize(this); // Не викликати деструктор
    }

    protected virtual void Dispose(bool disposing)
    {
        if (!_disposed)
        {
            if (disposing)
            {
                // Звільнити управляні ресурси
            }
            // Звільнити неуправляні ресурси
            _disposed = true;
        }
    }

    ~MyResource()
    {
        Dispose(false); // Резервне очищення
    }
}
```

### Використання:
```csharp
// Явне використання
using (var resource = new MyResource())
{
    // Робота з ресурсом
} // Dispose викликається автоматично

// Або
var resource = new MyResource();
try
{
    // Робота з ресурсом
}
finally
{
    resource?.Dispose();
}
```

### using statement (C# 8.0+):
```csharp
using var resource = new MyResource();
// Робота з ресурсом
// Dispose викликається в кінці області видимості
```

### IAsyncDisposable (.NET Core 3.0+)

Для асинхронного очищення ресурсів використовуйте `IAsyncDisposable`:

```csharp
public class AsyncResource : IAsyncDisposable
{
    public async ValueTask DisposeAsync()
    {
        // Асинхронне звільнення ресурсів
        await _stream.DisposeAsync();
    }
}

// Використання
await using var resource = new AsyncResource();
// DisposeAsync викликається автоматично
```

### Коли використовувати IDisposable:
- Робота з файлами (FileStream, StreamReader)
- Робота з базами даних (SqlConnection)
- Робота з мережею (HttpClient, TcpClient)
- Робота з графікою (Graphics, Bitmap)
- Будь-які неуправляні ресурси

### Коли використовувати IAsyncDisposable:
- Асинхронна робота з файлами (StreamWriter, StreamReader)
- Асинхронна робота з мережею (HttpClient, TcpClient)
- Будь-які ресурси, що потребують асинхронного очищення

---

## Клас System.GC

### Теорія

**System.GC** - статичний клас для взаємодії з Garbage Collector.

### Основні методи:

#### GC.Collect()
```csharp
GC.Collect(); // Збірка всіх поколінь
GC.Collect(0); // Збірка тільки покоління 0
GC.Collect(2, GCCollectionMode.Forced); // Примусова збірка
```

#### GC.WaitForPendingFinalizers()
```csharp
GC.Collect();
GC.WaitForPendingFinalizers(); // Чекати завершення фіналізації
GC.Collect(); // Повторна збірка
```

#### GC.GetGeneration()
```csharp
var obj = new MyClass();
int generation = GC.GetGeneration(obj); // Отримати покоління об'єкта
```

#### GC.GetTotalMemory()
```csharp
long memory = GC.GetTotalMemory(false); // Поточна використана пам'ять
```

#### GC.SuppressFinalize()
```csharp
GC.SuppressFinalize(this); // Скасувати виклик деструктора
```

### Важливо:
⚠️ **Не викликайте GC.Collect() в продакшн коді без потреби!**
- GC оптимізований для автоматичної роботи
- Примусова збірка може погіршити продуктивність
- Використовуйте тільки для тестування або в особливих випадках

---

## Поняття поколінь при збиранні сміття

### Теорія

.NET використовує **покоління (generations)** для оптимізації збирання сміття.

### Три покоління:

#### Generation 0 (Gen 0)
- **Нові об'єкти** (тільки що створені)
- **Найчастіша збірка** (найшвидша)
- Розмір: ~16 MB (залежить від платформи)
- **Більшість об'єктів помирають тут**

#### Generation 1 (Gen 1)
- Об'єкти, які **пережили збірку Gen 0**
- Середня частота збирання
- Розмір: ~128 MB
- **Буфер між Gen 0 і Gen 2**

#### Generation 2 (Gen 2)
- **Довгоживучі об'єкти**
- **Рідкісна збірка** (найповільніша)
- Може бути дуже великим
- Об'єкти з деструкторами завжди в Gen 2

### Large Object Heap (LOH)
- Об'єкти **більше 85,000 байт**
- Не проходять через покоління
- Збирається тільки при повній збірці (full GC)

### Алгоритм роботи:
1. Об'єкт створюється в **Gen 0**
2. Після збірки Gen 0, об'єкти, що вижили, переходять в **Gen 1**
3. Після збірки Gen 1, об'єкти, що вижили, переходять в **Gen 2**
4. Об'єкти в Gen 2 залишаються там до повної збірки

### Переваги поколінь:
✅ **Швидкість** - більшість об'єктів помирають в Gen 0 (швидко)
✅ **Локалізація** - нові об'єкти згруповані разом
✅ **Ефективність** - GC не перевіряє всі об'єкти кожного разу

---

## Практичні приклади

### Приклад 1: Життєвий цикл об'єкта

```csharp
// Об'єкт створюється в Gen 0
var obj = new MyClass();

// Об'єкт використовується
obj.DoSomething();

// Втрата посилання - об'єкт стає кандидатом на видалення
obj = null;

// GC може видалити об'єкт при наступній збірці
```

### Приклад 2: Відстеження поколінь

```csharp
var obj = new MyClass();
Console.WriteLine($"Після створення: Gen {GC.GetGeneration(obj)}");

GC.Collect(0);
Console.WriteLine($"Після збірки Gen 0: Gen {GC.GetGeneration(obj)}");

GC.Collect(1);
Console.WriteLine($"Після збірки Gen 1: Gen {GC.GetGeneration(obj)}");
```

### Приклад 3: Використання IDisposable

```csharp
// Автоматичне викликання Dispose
using (var resource = new FileResource("test.txt"))
{
    resource.WriteData("Hello, World!");
} // Dispose викликається тут

// Сучасний синтаксис (C# 8.0+)
using var resource2 = new FileResource("test2.txt");
resource2.WriteData("Data");
// Dispose викликається в кінці області видимості
```

### Приклад 4: Моніторинг пам'яті

```csharp
long memoryBefore = GC.GetTotalMemory(false);
Console.WriteLine($"Пам'ять до: {memoryBefore / 1024} KB");

// Створення об'єктів
for (int i = 0; i < 1000; i++)
{
    var obj = new MyClass();
}

long memoryAfter = GC.GetTotalMemory(false);
Console.WriteLine($"Пам'ять після: {memoryAfter / 1024} KB");
Console.WriteLine($"Різниця: {(memoryAfter - memoryBefore) / 1024} KB");
```

### Приклад 5: Деструктор vs Dispose

```csharp
// Об'єкт з деструктором
var objWithFinalizer = new ClassWithFinalizer();
objWithFinalizer = null;
GC.Collect();
GC.WaitForPendingFinalizers(); // Чекати виконання деструктора

// Об'єкт з Dispose
using (var objWithDispose = new ClassWithDispose())
{
    // Робота з об'єктом
} // Dispose викликається миттєво
```

### Приклад 6: IAsyncDisposable (сучасний підхід)

```csharp
// Асинхронне очищення ресурсів
await using var asyncResource = new AsyncResource("file.txt");
await asyncResource.WriteDataAsync("Data");
// DisposeAsync викликається автоматично в кінці області видимості

// Або з явним блоком
await using (var resource = new AsyncResource("file2.txt"))
{
    await resource.WriteDataAsync("Data");
} // DisposeAsync викликається тут
```

---

## Рекомендації та найкращі практики

### ✅ Рекомендується:
1. **Використовуйте IDisposable** для ресурсів, що потребують очищення
2. **Використовуйте using** для автоматичного викликання Dispose
3. **Реалізуйте правильний паттерн Dispose** для класів з неуправляними ресурсами
4. **Дозвольте GC працювати автоматично** - не викликайте GC.Collect() без потреби

### ❌ Не рекомендується:
1. **Не використовуйте деструктори** для звичайних об'єктів
2. **Не викликайте GC.Collect()** в продакшн коді
3. **Не тримайте посилання** на об'єкти, які більше не потрібні
4. **Не створюйте зайві об'єкти** в циклах (особливо великі)

### 🔍 Діагностика проблем з пам'яттю:
- Використовуйте **dotMemory**, **PerfView**, або **Visual Studio Diagnostic Tools**
- Моніторте **GC.GetTotalMemory()** для відстеження використання пам'яті
- Перевіряйте **GC.GetGeneration()** для розуміння життєвого циклу об'єктів

---

## Сучасні концепції .NET (актуальні на 2024-2025)

### 1. **GC Regions** (.NET Core 2.1+)
- Покращена локальність об'єктів
- Більш ефективне використання пам'яті

### 2. **Background GC**
- Збірка сміття в фоновому потоці
- Мінімальні паузи в роботі програми

### 3. **Server GC vs Workstation GC**
- Автоматичний вибір в .NET Core
- Можна налаштувати через конфігурацію

### 4. **Span<T> та Memory<T>**
- Ефективна робота з пам'яттю
- Менше алокацій на heap

### 5. **ArrayPool<T>**
- Перевикористання масивів
- Зменшення навантаження на GC

### 6. **IAsyncDisposable** (.NET Core 3.0+)
- Асинхронне очищення ресурсів
- Використовується з `await using`
- Дозволяє асинхронно звільняти ресурси без блокування потоків

#### Приклад використання:
```csharp
await using var asyncResource = new AsyncResource("file.txt");
await asyncResource.WriteDataAsync("Data");
// DisposeAsync викликається автоматично
```

---

## Висновки

1. **GC автоматично керує пам'яттю** - не потрібно явно звільняти об'єкти
2. **IDisposable для ресурсів** - використовуйте для файлів, з'єднань тощо
3. **Покоління оптимізують GC** - більшість об'єктів помирають в Gen 0
4. **Деструктори - рідкість** - використовуйте тільки для неуправляних ресурсів
5. **Довіртеся GC** - він оптимізований для автоматичної роботи

---

## Додаткові ресурси

- [Microsoft Docs: Garbage Collection](https://learn.microsoft.com/en-us/dotnet/standard/garbage-collection/)
- [Microsoft Docs: IDisposable Pattern](https://learn.microsoft.com/en-us/dotnet/standard/garbage-collection/implementing-dispose)
- [.NET Memory Management](https://learn.microsoft.com/en-us/dotnet/standard/garbage-collection/fundamentals)

---

**Автор:** Матеріали для викладання .NET BASE  
**Дата:** 2024-2025  
**Версія .NET:** 10.0

