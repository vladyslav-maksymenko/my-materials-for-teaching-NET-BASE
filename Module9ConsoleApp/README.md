# Модуль 9. Вступ до Generics, Ітераторів та Колекцій

## Generics (Узагальнення)

### Що таке Generics?

**Generics** (узагальнення) - це потужний механізм у C# (починаючи з .NET Framework 2.0), який дозволяє створювати елементи (класи, інтерфейси, методи, делегати), що працюють з типами, визначеними користувачем під час використання, а не під час написання коду. Це також відомо як `параметризований` тип або `типізований об'єкт`.

**Синтаксис:** `ClassName<T>` де `T` - це параметр типу (type parameter), який буде замінено конкретним типом під час компіляції/виконання.

Узагальнені типи дуже легко розпізнати. Вони вирізняються наявністю кутових дужок `<>` після назви типу, усередині яких зазначається певний тип або типи даних.

### Необхідність та Переваги Generics
Введення Generics було спровоковано проблемами, що виникають під час роботи з неузагальненими колекціями, такими як `ArrayList`, які зберігають елементи типу `object`. 

Можна розцінювати як етап еволюції мови C#, спровокований двома важливими проблемами, що виникають під час роботи з неузагальненими колекціями - низька продуктивність і безпека типів.

Проблема безпеки типів пов'язана з універсальністю неузагальнених колекцій - зберігання даних типу object. Така гнучкість у деяких випадках зручна, коли в колекцію необхідно помістити змінні будь-якого типу. Однак, під час вилучення даних із колекції необхідно здійснювати явне приведення типу. Водночас можна легко припуститися помилки приведення, яка виявиться тільки на етапі виконання програми. Тому робота з неузагальненими колекціями повинна супроводжуватися написанням додаткового коду перевірок і приведення — блок try-catch або оператори is чи as

```csharp
class Program
    {
        static void Main(string[] args)
        {
            ArrayList arrayList = new ArrayList();
            // поміщаємо в колекцію елементи типу int
            arrayList.Add(10);
            try
            {
                // під час вилучення виконуємо приведення до типу short
                // через помилкове зазначення типу виникає виняток
                short a = (short)arrayList[0];
            }
            catch (InvalidCastException e)
            {
                WriteLine(e.Message);
            }
        }
    }
```
| Проблема без Generics | Перевага Generics |
| :--- | :--- |
| **Втрата типобезпеки** та помилки під час виконання (явне приведення типів). | ✅ **Типобезпека**. Помилки виявляються на етапі компіляції. |
| **Низька продуктивність** через Boxing (пакування) та Unboxing (розпакування) для значущих типів. | ✅ **Покращена продуктивність.** Усунення boxing/unboxing для значущих типів. |
| **Дублювання коду** (необхідність створювати окремі класи/методи для кожного типу). | ✅ **Повторне використання коду.** Один клас/метод для різних типів. |
| Складніший, менш зрозумілий код. | ✅ **Краща читабельність** та лаконічність. |

    Наприклад, до Generics, якщо ви зберігали цілі числа в ArrayList, компілятор не знав, що там цілі числа, і міг дозволити додати рядок. Generics дозволяють створити List<int>, де гарантовано будуть лише цілі числа.

### Механізм Boxing (Пакування) та Unboxing (Розпакування)

Це ключовий аспект, що пояснює перевагу Generics у продуктивності.

Пакування (Boxing): Перетворення значущого типу (наприклад, int) на посилальний тип (object). Це відбувається, коли значущий тип зберігається в колекції, яка очікує object. Це вимагає виділення пам'яті в купі (Heap) та копіювання значення.

Розпакування (Unboxing): Зворотне перетворення посилального типу на значущий тип. Потрібне явне приведення типів.

```csharp
class Program
    {
        static void Main(string[] args)
        {
            object obj = 45; // boxing
            WriteLine($"Пакування: {obj}");

            int number = (int)obj; // unboxing(необхідність здійснення явного приведення типів, інакше згенерується помилка на етапі компіляції)
            WriteLine($"Розпакування: {number}");
        }
    }
```
Розглянемо код нижче:
```csharp
 class Program
    {
        static void Main(string[] args)
        {
            ArrayList arrayList = new ArrayList();
            // під час запису елемента в колекцію тип int приводиться в object - ПАКУВАННЯ
            arrayList.Add(10);
            arrayList.Add(12);

            // під час вилучення виконуємо приведення типу - РОЗПАКУВАННЯ
            int a = (int)arrayList[0];
            // виведення значення - int приводиться до string - ПАКУВАННЯ
            Write($"Перший елемент колекції: {a}");

            WriteLine("\n\nВсі елементи колекції:");
            foreach (int item in arrayList)
            {
                WriteLine($"\t{item}"); // ПАКУВАННЯ
            }
        }
    }
```
Коли ви використовуєте `List<int>` замість `ArrayList`, пакування/розпакування не відбувається, що значно підвищує продуктивність і зменшує навантаження на збирач сміття (GC).

### Порівняння Generic's та Неузагальнених Колекцій
З появою концепції Generics у FCL (Framework Class Library), практично для всіх класів неузагальнених колекцій з'явилися відповідні їм узагальнені аналоги.

Місце розташування: Ці узагальнені колекції, а також узагальнені інтерфейси, знаходяться в просторі імен `System.Collections.Generic`.

| Generic-колекції (`System.Collections.Generic`) | Неузагальнені колекції (`System.Collections`) |
| :--- | :--- |
| `List<T>` | `ArrayList` |
| `Dictionary<TKey, TValue>` | `Hashtable` (Хеш-таблиця) |
| `SortedList<TKey, TValue>` | `SortedList` |
| `Stack<T>` | `Stack` |
| `Queue<T>` | `Queue` |

Головна відмінність generic-колекцій від неузагальнених колекцій (крім назв, що не завжди збігаються) полягає в тому, що замість типу даних object у generic-колекціях використовується узагальнений параметр типу, який зазначено в кутових дужках після назви колекції. У результаті ті методи і властивості, які в неузагальненій колекції використовували тип даних object, у generic-колекції використовують конкретний тип даних.
```csharp
 class Program
    {
        static void Main(string[] args)
        {
            List<int> listInt = new List<int> { 53, 12, 78 };
            listInt.Add(8.9); //Помилка: не можна конвертувати тип double в int
        }
    }
```
Для порівняння часових витрат при зберіганні структурних типів у неузагальненій та узагальненій колекції ми наведемо приклад, запропонований Jeffrey Richter у книзі "The CLR via C#".

<details>
<summary>Показати/Приховати Код Тестування Продуктивності (Generics vs ArrayList)</summary>

```csharp
using System;
using System.Collections;
using System.Collections.Generic;
using System.Diagnostics;
using static System.Console;

namespace SimpleProject
{
    /// <summary>
    /// Допоміжний клас для профілювання ділянки коду
    /// виконує вимірювання часу виконання
    /// і підрахунок кількості збирань сміття
    /// </summary>
    internal sealed class OperationTimer : IDisposable
    {
        long _startTime;
        string _text;
        int _collectionCount;

        public OperationTimer(string text)
        {
            PrepareForOperation();
            _text = text;

            // зберігається кількість збирань сміття, виконаних на поточний момент
            _collectionCount = GC.CollectionCount(0);

            // зберігається початковий час
            _startTime = Stopwatch.GetTimestamp();
        }

        /// <summary>
        /// Викликається в разі руйнування об'єкта
        /// Виводить:
        /// значення часу, що минув з моменту створення об'єкта 
        /// до моменту його видалення
        /// кількість виконаних збирань сміття, виконаних за цей час
        /// </summary>

        public void Dispose()
        {
            WriteLine($"{_text}\t{(Stopwatch.GetTimestamp() - _startTime) / (double)Stopwatch.Frequency:0.00} секунди (збирань сміття {GC.CollectionCount(0) - _collectionCount})");
        }
        /// <summary>
        /// Метод видаляються всі невикористовувані об'єкти
        /// Це потрібно для "чистоти експерименту",
        /// тобто, щоб збирання сміття відбувалося тільки для об'єктів,
        /// які створюватимуться в профільованому блоці коду
        /// </summary>
        private static void PrepareForOperation()
        {
            GC.Collect();
            GC.WaitForPendingFinalizers();
            GC.Collect();
        }
    }
    class Program
    {
        /// <summary>
        /// метод для тестування продуктивності
        /// узагальненого і неузагальненого списку
        /// </summary>
        private static void ValueTypePerfTest()
        {
            const int COUNT = 10000000;
            // об'єкт OperationTimer 
            // створюється перед початком використання колекції
            // після завершення її використання 
            // виводить час, витрачений на роботу з колекцією  
            using (new OperationTimer("List"))
            {
                // використання узагальненого списку
                List<int> list = new List<int>(COUNT);
                for (int n = 0; n < COUNT; n++)
                {
                    list.Add(n);
                    int x = list[n];
                }
                list = null; // для гарантованого виконання збирання сміття
            }

            using (new OperationTimer("ArrayList"))
            {
                // використання неузагальненого списку
                ArrayList array = new ArrayList();
                for (int n = 0; n < COUNT; n++)
                {
                    array.Add(n); // виконується пакування
                    int x = (int)array[n]; // виконується розпакування
                }
                array = null; // для гарантованого виконання збирання сміття
            }
        }
        static void Main(string[] args)
        {
            ValueTypePerfTest();
        }
    }
}
```
Результати можуть відрізнятися від версії .NET та від параметрів комп'ютера:
- List    0,20 секунди (збирань сміття 0)
- ArrayList       3,04 секунди (збирань сміття 47)

</details>


### Створення Generic-класів
Коли створюється generic-клас, параметр типу вказується в кутових дужках після імені класу. Потім цей тип використовується також як звичайні типи. Узагальнених параметрів типу може бути й кілька.

Під час використання узагальненого класу замість параметра типу підставляють реальний тип даних. Створення конкретного екземпляра generic-класу називається інстанціюванням або спеціалізацією.

Оскільки параметр типу заздалегідь невідомий, то не можна визначитися зі значенням за замовчуванням для змінних узагальненого типу. Тому для завдання значення за замовчуванням параметру типу використовується ключове слово default(T). При цьому значенням посилального типу присвоюється null, а структурного — 0.
```csharp
public class Box<T>
{
    private T item;
    
    public void SetItem(T item) { this.item = item; }
    public T GetItem() { return item; }

    // Значення за замовчуванням: default(T)
    // T = null для посилальних типів, T = 0 для значущих
    public Box()
    {
        item = default(T);
    }
}

Box<int> intBox = new Box<int>(); 
Box<string> stringBox = new Box<string>(); 
// JIT-компілятор створює спеціалізовані типи, наприклад, Box`1[System.Int32] - Ці класи називаються constructed type (сконструйований тип)
```
Зі змінними узагальненого типу не можна виконувати арифметичні дії та операції порівняння. Це пов'язано з тим, що під час інстанціювання замість параметра типу може бути використано тип даних, який не підтримує ці операції.

Generic-класи можуть використовуватися як базові класи. Отже, generic-класи можуть мати віртуальні та абстрактні методи. Однак, необхідно дотримуватися таких правил успадкування від generic-класів:
- якщо від generic-класу успадковується неузагальнений клас, спадкоємець повинен конкретизувати параметр типу;
- у разі реалізації generic віртуальних методів похідний неузагальнений клас повинен конкретизувати параметр типу;
- якщо від generic-класу успадковується інший generic-клас, у ньому необхідно враховувати обмеження типу, зазначені в базовому класі.

```csharp
namespace SimpleProject
{
    class GenericClass<T>
    {
        public void M1(T obj)
        {
            WriteLine($"Параметр: {obj}");
        }

        public virtual void M2(T data)
        {
            WriteLine($"Generic: {data}");
        }
    }

    class InheritClass : GenericClass<int> // необхідно явно вказувати тип
    {
        public override void M2(int data)
        {
            WriteLine($"Inherit: {data}");
        }
    }

    class Program
    {
        static void Main()
        {
            InheritClass obj = new InheritClass();
            obj.M1(45);
            obj.M2(82);
        }
    }
}
```

Успадкування узагальненого класу від звичайного класу не пов'язане з будь-якими обмеженнями

```csharp
namespace SimpleProject
{
    class BasicClass
    {
        protected int age;
    }

    class GenericClass<T> : BasicClass
    {
        public void M1(T obj)
        {
            age = 57;
            WriteLine($"Basic: {age}\nGeneric: {obj}");
        }
    }

    class Program
    {
        static void Main()
        {
            GenericClass<int> obj = new GenericClass<int>();
            obj.M1(45);
        }
    }
}
```
### Типи, вкладені всередину Generic-класу

Усередині generic-класу можна оголошувати вкладені класи. Ці класи також будуть узагальненими, навіть якщо вони не мають власних параметрів типу. У вкладених класах можна використовувати параметр типу, вказаний у зовнішньому класі. У вкладеному класі можна також оголошувати власний список параметрів типу. У цьому разі вкладений клас буде параметризований двома наборами типів — власним і зовнішнього класу:

```csharp
namespace SimpleProject
{
    class A<T>
    {
        public class Inner
        {
        }
    }

    class B<T>
    {
        // вкладений клас має власний список параметрів типу
        public class Inner<U>
        {
        }
    }

    class Program
    {
        static void Main(string[] args)
        {
            // для використання вкладеного класу 
            // необхідно вказати реальний тип замість параметра типу зовнішнього класу
            A<int>.Inner a = new A<int>.Inner();
            WriteLine(a);
            A<double>.Inner a1 = new A<double>.Inner();
            WriteLine(a1);

            // для використання вкладеного класу 
            // необхідно вказати реальний тип замість параметра типу вкладеного класу
            B<int>.Inner<string> b = new B<int>.Inner<string>();
            WriteLine(b);
        }
    }
}
```

### Використання обмежень (Constraints)

Обмеження дозволяють вказати вимоги до параметрів типів:

- `where T : class` - T має бути reference типом
- `where T : struct` - T має бути value типом
- `where T : new()` - T має мати конструктор без параметрів
- `where T : BaseClass` - T має наслідуватися від BaseClass
- `where T : IInterface` - T має реалізувати IInterface
- `where T : U` - T має наслідуватися від U

**Приклад:**
```csharp
public class Repository<T> where T : class, IEntity, new()
{
    // T має бути класом, реалізувати IEntity та мати конструктор без параметрів
}
```

## Generic-Інтерфейси, Делегати та Методи
Як уже зазначалося, у просторі імен System.Collections.Generic містяться узагальнені інтерфейси, які відповідають неузагальненим інтерфейсам
### 🏷️ Відповідність Інтерфейсів Колекцій

| Generic-інтерфейси | Неузагальнені інтерфейси |
| :--- | :--- |
| `IList<T>` | `IList` |
| `IDictionary<TKey, TValue>` | `IDictionary` |
| `ICollection<T>` | `ICollection` |
| `IEnumerator<T>` | `IEnumerator` |
| `IComparer<T>` | `IComparer` |
| `IComparable<T>` | `IComparable` |

У разі потреби реалізації будь-якого інтерфейсу перевагу слід віддавати узагальненим інтерфейсам. Це не примха і не порада, а точний розрахунок.

```csharp
public interface IRepository<T>
{
    void Add(T item);
    T GetById(int id);
}

public class DateComparer : IComparer<Student> // Узагальнений інтерфейс
{
    public int Compare(Student x, Student y)
    {
        // ...
    }
}
```
### Створення Generic-делегатів

```csharp
public delegate T Transformer<T>(T input);
// Вбудовані:
// public delegate TResult Func<T, TResult>(T arg);
// public delegate void Action<T>(T arg);
```

### Створення Generic-методів
У деяких випадках зручно мати метод, параметризований якимось типом даних. Такий метод може перебувати як в узагальненому, так і в неузагальненому класі. У разі створення generic-методу параметр типу вказується в кутових дужках після імені методу.
```csharp
public class Printer
{
    // Метод Print є generic, він приймає параметр типу T
    public static void Print<T>(T value)
    {
        Console.WriteLine($"Тип: {value.GetType().Name}, Значення: {value}");
    }
}

// Використання:
// Компілятор C# автоматично виводить тип (type inference)
Printer.Print(100);             // Виклик: T - це int
Printer.Print("Hello Generics"); // Виклик: T - це string
Printer.Print(3.14m);           // Виклик: T - це decimal
```
Check point for me - go to the IDE (show more examples - start from 4) - DEMO

### Generics та Варіантність (Covariance & Contravariance) - OUT OF PROGRAM
Варіантність дозволяє використовувати більш конкретні (для коваріантності) або менш конкретні (для контраваріантності) типи, ніж ті, що вказані параметром типу. Це застосовується лише до generic-інтерфейсів та делегатів.

### 🔄 Варіантність (Covariance and Contravariance)

| Варіантність | Ключове слово | Опис | Застосовується до (Приклади) |
| :--- | :--- | :--- | :--- |
| **Коваріантність** | `out` | Дозволяє використовувати більш **конкретний (похідний)** тип. Використовується для типів, які лише **повертаються** з методів (вихідні параметри). | `IEnumerable<out T>`, `IComparable<out T>` |
| **Контраваріантність** | `in` | Дозволяє використовувати менш **конкретний (базовий)** тип. Використовується для типів, які лише **приймаються** методами (вхідні параметри). | `IComparer<in T>`, `IAction<in T>` |

Приклад (Коваріантність):

Якщо string успадковується від object, то IEnumerable<string> коваріантний до IEnumerable<object>.

```csharp
IEnumerable<string> strings = new List<string>();
// OK, коваріантність дозволяє призначити більш конкретний тип (string) 
// менш конкретному (object) для IEnumerable<T>
IEnumerable<object> objects = strings;
```

---

## Ітератори (Iterators)
Уявіть, що ви переглядаєте список контактів у телефоні:
## 📖 Інтерфейси Ітерації: IEnumerable vs IEnumerator

| Інтерфейс | Аналогія | Роль | Що він робить |
| :--- | :--- | :--- | :--- |
| **`IEnumerable<T>`** | **Телефонна книга** (весь список контактів). | Колекція, яку можна перебирати. | Описує, що об'єкт може бути ітерований (обійдений). Його єдиний важливий метод: `GetEnumerator()`. |
| **`IEnumerator<T>`** | **Палець**, який вказує на поточний контакт. | Механізм перебору (курсор). | Описує, як саме відбувається обхід. Він має властивості/методи: `Current`, `MoveNext()`, `Reset()`. |

### Визначення: Що таке IEnumerable<T>? (Колекція)
- Призначення: Це об'єкт, який гарантує, що він може видати вам ітератор (курсор) для початку обходу.
- Метод: IEnumerator<T> GetEnumerator();
- Використання: Ви його не викликаєте прямо. Цикл foreach автоматично викликає GetEnumerator() на початку, щоб отримати курсор.

### Визначення: Що таке IEnumerator<T>? (Курсор/Ітератор)
- Призначення: Це об'єкт, який фактично виконує роботу з переходу від елемента до елемента.
- Методи/Властивості:
    - bool MoveNext(): Переміщує курсор до наступного елемента. Повертає true, якщо елемент знайдено, і false, якщо кінець колекції.
    - T Current: Повертає елемент, на який зараз вказує курсор.

### Схема Роботи Циклу foreach
```csharp
//Коли ви пишете
foreach (Student student in auditory)
{
    // ...
}

//Комп'ютер виконує приблизно такий код:

// 1. Цикл викликає GetEnumerator() для отримання курсора (IEnumerator<T>)

IEnumerator<Student> enumerator = auditory.GetEnumerator(); 

try 
{
    // 2. Цикл перевіряє, чи є наступний елемент
    while (enumerator.MoveNext()) 
    {
        // 3. Зберігає поточний елемент у змінній
        Student student = enumerator.Current; 
        
        // ... Ваш код
    }
}
finally 
{
    // 4. Очищення ресурсів
    if (enumerator is IDisposable disposable) 
    {
        disposable.Dispose();
    }
}
```
### Роль yield return
А тут вступає ітератор з yield return, який усуває потребу писати весь цей нудний код з IEnumerator<T> вручну.
Якщо ваш метод повертає IEnumerable<T> і містить yield return, компілятор C# автоматично генерує за вас цілий допоміжний клас, який реалізує IEnumerator<T> та IEnumerable<T>!

### Що таке ітератор?

**Ітератор** - це блок коду, який використовується для визначення способу обходу колекції. Ітератори дозволяють обійти колекцію, не створюючи при цьому тимчасової копії всієї колекції в пам'яті. 

**Переваги:**
- Ліниве виконання (lazy evaluation)
- Ефективне використання пам'яті
- Простий синтаксис

Ітератори дозволяють:

- **Лінивий обхід (Deferred Execution)**: Елементи обчислюються та повертаються лише тоді, коли їх запитує цикл foreach, а не всі одразу. Це економить пам'ять та час.

- **Збереження стану**: Ітератор "запам'ятовує" свій стан між викликами, дозволяючи продовжити обхід з місця зупинки.

- 💡 К**лючовий зв'язок**: Для того, щоб клас міг бути ітерований за допомогою foreach, він повинен реалізувати узагальнений інтерфейс IEnumerable<T> (або неузагальнений IEnumerable). Якщо ви використовуєте ітераторний метод з yield return, компілятор C# автоматично генерує за вас увесь необхідний допоміжний код для реалізації IEnumerable<T> та IEnumerator<T>.

**Основні ключові слова:**
* `yield return`: Використовується для повернення одного елемента за раз під час ітерації. Це дозволяє методу-ітератору "запам'ятати" свій стан і продовжити виконання з місця зупинки наступного разу, коли буде викликано наступний елемент (зазвичай у циклі foreach).
* `yield break` - завершує ітерацію

Приклад (Звичайний ітератор, який реалізує GetEnumerator):
```csharp
public IEnumerator<Student> GetEnumerator()
{
    // Компілятор автоматично генерує клас, 
    // який реалізує IDisposable, IEnumerator<Student> та IEnumerable<Student>
    for (int i = 0; i < _auditory.Count; i++)
    {
        yield return _auditory[i]; // Повертає елемент і призупиняє виконання
    }
}
```
**Іменовані ітератори** – це просто методи всередині класу, які використовують yield return і мають тип повернення IEnumerable<T> (або IEnumerable). Вони дозволяють створити кілька різних способів обходу однієї й тієї ж колекції.
```csharp
public IEnumerable<T> Ім'я_Ітератора(список_параметрів)
{
    // ...
    yield return повернуте_значення;
}
```
---

## Колекції (Collections)

### Поняття колекції

**Колекція** - це набір об'єктів, згрупованих за певною ознакою. На відміну від статичних масивів, більшість колекцій є динамічними, тобто вони можуть змінювати свій розмір під час виконання програми.
###  📁 Основні Простори Імен для Колекцій у .NET
| Простір імен | Тип колекцій | Основна особливість |
| :--- | :--- | :--- |
| **`System.Collections`** | Неузагальнені (Non-Generic) | Зберігають елементи типу `object`. Викликають boxing/unboxing для значущих типів, низька типобезпека. |
| **`System.Collections.Generic`** | Узагальнені (Generic) | Зберігають елементи конкретного, заданого типу (`T`). Типобезпечні, висока продуктивність (усунення boxing/unboxing). |
| **`System.Collections.Concurrent`** | Багатопотокові | Колекції, оптимізовані та безпечні для роботи в багатопотоковому середовищі (наприклад, `ConcurrentDictionary`). |
| **`System.Collections.Specialized`** | Спеціалізовані | Колекції з особливою логікою (наприклад, оптимізовані для малого розміру, або для зберігання рядків, як `StringCollection`). |

### Generic-колекції

Generic-колекції забезпечують типобезпеку та кращу продуктивність порівняно з не-generic колекціями.

### Класи колекцій

#### 1. List<T>
Динамічний масив, що дозволяє додавати, видаляти та змінювати елементи.

**Основні методи:**
- `Add(T item)` - додає елемент
- `Remove(T item)` - видаляє елемент
- `Contains(T item)` - перевіряє наявність
- `Count` - кількість елементів

#### 2. Dictionary<TKey, TValue>
Колекція ключ-значення для швидкого пошуку за ключем.

**Основні методи:**
- `Add(TKey key, TValue value)` - додає пару
- `ContainsKey(TKey key)` - перевіряє наявність ключа
- `TryGetValue(TKey key, out TValue value)` - безпечне отримання значення

#### 3. Stack<T>
Колекція типу LIFO (Last In, First Out) - останній прийшов, перший вийшов.

**Основні методи:**
- `Push(T item)` - додає на вершину
- `Pop()` - видаляє з вершини
- `Peek()` - переглядає вершину без видалення

#### 4. Queue<T>
Колекція типу FIFO (First In, First Out) - перший прийшов, перший вийшов.

**Основні методи:**
- `Enqueue(T item)` - додає в кінець
- `Dequeue()` - видаляє з початку
- `Peek()` - переглядає початок без видалення

#### 5. SortedList<TKey, TValue>
Автоматично відсортований словник за ключами.

#### 6. HashSet<T>
Колекція унікальних елементів без дублікатів.

#### 7. LinkedList<T>
Двозв'язний список для ефективного додавання/видалення в будь-якому місці.

### Інтерфейси колекцій

#### IList<T>
Інтерфейс для списків з індексацією:
- `this[int index]` - індексатор
- `Insert(int index, T item)` - вставка
- `RemoveAt(int index)` - видалення за індексом

#### IEnumerable<T>
Інтерфейс для перебору елементів:
- `GetEnumerator()` - повертає ітератор

#### IEnumerator<T>
Інтерфейс ітератора:
- `Current` - поточний елемент
- `MoveNext()` - перехід до наступного
- `Reset()` - скидання

#### ICollection<T>
Базовий інтерфейс колекцій:
- `Count` - кількість елементів
- `Add(T item)`, `Remove(T item)`, `Clear()`, `Contains(T item)`

#### IDictionary<TKey, TValue>
Інтерфейс словників:
- `Keys` - колекція ключів
- `Values` - колекція значень
- `this[TKey key]` - індексатор за ключем

#### IComparer<T>
Інтерфейс для порівняння об'єктів:
- `Compare(T x, T y)` - порівняння двох об'єктів

### Приклади використання

Дивіться детальні приклади в файлах коду з коментарями та поясненнями.

---

## Структура проєкту

- `Program.cs` - головний файл з демонстрацією всіх концепцій
- `GenericExamples.cs` - приклади Generic-класів, інтерфейсів, делегатів та методів
- `IteratorExamples.cs` - приклади ітераторів
- `CollectionExamples.cs` - приклади роботи з колекціями

---

## Як запустити

```bash
dotnet build
dotnet run
```

